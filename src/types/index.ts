import { Languages, LngParam } from '@/types/i18n';
import { SignUpData, TokenRes } from '@/types/auth';
import { Annotations, JSONError } from '@/types/bodyEditor';

export type { LngParam, Languages, TokenRes, SignUpData, Annotations, JSONError };
