import { Languages, LngParam } from '@/types/i18n';
import { SignUpData, TokenRes } from '@/types/auth';
import { Annotations, JSONError, EditorOptions } from '@/types/bodyEditor';
import { Method, MenuTabsRest, BodyType, PlaceHolder, SegmentIndex, BodyMode } from '@/types/enum';

export type { LngParam, Languages, TokenRes, SignUpData, Annotations, JSONError, EditorOptions };

export { Method, MenuTabsRest, BodyType, PlaceHolder, SegmentIndex, BodyMode };
